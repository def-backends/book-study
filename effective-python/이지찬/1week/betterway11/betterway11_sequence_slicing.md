## 시퀀스를 슬라이싱 하는 방법을 익혀라
```bash
1. 슬라이싱은 간결하게, 시작 인덱스에 0, 마지막 인덱스에 시퀀스 길이를 사용하지 마라.
2. 슬라이싱은 범위를 넘어가는 인덱스를 허용한다.
3. 리스트 슬라이스에 대입하면 원래 시퀀스에서 슬라이스가 가리키는 부분을 대입 연산자 오른쪽에 있는 시퀀스로 대치한다
   e.g) a = [1, 2, 3, 4, 5] 라고 하고 a[1:3] = [6, 7, 8] 이라고 하면 a는 [1, 6, 7, 8, 4, 5]가 된다.
```