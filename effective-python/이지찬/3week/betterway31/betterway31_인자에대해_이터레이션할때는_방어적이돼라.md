## 인자에 대해 이터레이션 할때는 방어적이 돼라
```bash
함수 인자로 이터러블한 객체를 받게 되었을때 인자값을 그대로 사용 하게 되었을경우 원본값이 변경될수 있다.
인자값으로 받은 값을 변경되는 예기치 않은 오류를 막기 위해서는 인자값으로 받은 이터러블한 객체를 복사하여 이후에 사용한다.
- 예를 들어 인자값으로 리스트를 받았을경우 .copy()를 사용하여 복사하여 사용한다.
추가적으로, 타입힌트, doc string, isinstance를 통해 인자값을 검증하고 사용하도록 하자.
```